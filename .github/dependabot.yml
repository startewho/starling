version: 2
updates:
  # Keep GitHub Actions pinned to SHAs but updated. Dependabot opens a PR
  # when a pinned action has a newer release, bumping the SHA and comment.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
