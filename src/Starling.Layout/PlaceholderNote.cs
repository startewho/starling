namespace Starling.Layout;

/// <summary>
/// Module status note. <see cref="LayoutEngine"/> performs block and inline
/// layout, floats, positioning, flexbox, grid, margin collapse, and word wrap.
/// Table formatting remains approximate through the user-agent stylesheet.
/// </summary>
public static class PlaceholderNote
{
    public const string Message =
        "Starling.Layout: block, inline, floats, positioning, flex, and grid ready. " +
        "Table formatting is approximate. See browser-plan/07_LAYOUT.md.";
}
