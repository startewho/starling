---
id: css-lists-3
url: https://www.w3.org/TR/css-lists-3/
title: CSS Lists Module Level 3
status: WD
generated: false
---

# CSS Lists Module Level 3

Spec: <https://www.w3.org/TR/css-lists-3/>

Conformance tests in this folder are hand-written and tagged with `[Spec(...)]`
plus either `[SpecFact]` (passes today) or `[PendingFact]` (a known gap).

Implemented (wp:M5-css-16): `list-style-type`/`-position`/`-image` longhands +
the `list-style` shorthand; UA defaults making `ul`/`ol`/`li` list items with
disc/decimal markers; marker text generation for disc/circle/square/decimal/
decimal-leading-zero/lower+upper-alpha/lower+upper-roman/none.

Deferred: `counter-reset`/`counter-increment`/`counter()`/`counters()`,
`@counter-style`, `list-style-image` rendering.
