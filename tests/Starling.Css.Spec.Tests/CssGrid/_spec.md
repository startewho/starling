---
id: css-grid
url: https://drafts.csswg.org/css-grid-2/
title: CSS Grid Layout Module Level 2
status: ED (snapshot via w3c/webref)
fetched: 2026-05-19
source: testdata/webref/css/css-grid.json
generated: false
definition_counts:
  properties: 15
  at_rules: 0
  selectors: 0
---

# CSS Grid Layout Module Level 2

Spec: <https://drafts.csswg.org/css-grid-2/>

Conformance tests in this folder are hand-written and tagged with
`[Spec(...)]` plus either `[SpecFact]` (passes today) or `[PendingFact]`
(a known gap we don't satisfy yet). There is no auto-generation — add a
test when you implement or discover a requirement, and flip `[PendingFact]`
to `[SpecFact]` once it passes. The upstream catalog of every property /
at-rule / selector the spec defines lives in `tasks/SPEC_CATALOG.md`.
