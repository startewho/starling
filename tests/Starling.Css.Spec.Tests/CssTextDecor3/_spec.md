---
id: css-text-decor-3
url: https://www.w3.org/TR/css-text-decor-3/
title: CSS Text Decoration Module Level 3
status: REC
fetched: 2026-05-20
source: hand-written (wp:M5-css-15)
generated: false
definition_counts:
  properties: 6
  at_rules: 0
  selectors: 0
---

# CSS Text Decoration Module Level 3

Spec: <https://www.w3.org/TR/css-text-decor-3/>

Conformance tests in this folder are hand-written and tagged with
`[Spec(...)]` plus either `[SpecFact]` (passes today) or `[PendingFact]`
(a known gap we don't satisfy yet). There is no auto-generation — add a
test when you implement or discover a requirement, and flip `[PendingFact]`
to `[SpecFact]` once it passes. The upstream catalog of every property /
at-rule / selector the spec defines lives in `tasks/SPEC_CATALOG.md`.

Covered: `text-decoration` shorthand expansion (§2.5), `text-decoration-line`
(§2.1), `text-decoration-style` (§2.2), `text-decoration-color` (§2.3),
`text-decoration-thickness` / `text-underline-offset`, and `text-shadow` (§5).
